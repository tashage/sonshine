using UnityEngine;
using System.Collections;

public class SpaceTurnOnLight : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		if (Input.GetKeyDown (KeyCode.Space)) {
			//turn the light's intensity to 1
				} 
		else {
			//turn the light's intensity to 0
				}
	}
}
